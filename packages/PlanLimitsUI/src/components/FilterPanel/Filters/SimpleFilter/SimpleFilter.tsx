// import React from 'react';
// import Dropdown, { DropdownValueType } from '@components/Dropdown/Dropdown';
// import { FilterDescriptor } from '@components/FilterPanel/FilterPanel';
//
// export const SimpleFilter: React.FC<FilterDescriptor> = (props) => {
//   const onSelection = (value: DropdownValueType) => onChange?.(value);
//
//   return (
//     <Dropdown {...props}
//       options={props.options || []}
//       onChange={onSelection}
//       value={props.currentValue as string}
//       placeholder={props.placeholder || "Select"}
//       dataTestid={`props.dropdown-${props.name}`}
//       className={`bg-transparent p-4 ${props.className}`}
//       controlClassName={'p-2'}
//     />
//   );
// };
//
// export default SimpleFilter;
