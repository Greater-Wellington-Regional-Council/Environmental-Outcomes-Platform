import { expect } from 'chai';
import FarmManagementUnit from './FarmManagementUnit';

describe('Spec FarmManagementUnit', function () {
    it('it exists', () => {
        expect(FarmManagementUnit).to.be.ok;
    });
});
