import EmailLink from './EmailLink';
// import { render, screen } from '@testing-library/react'
import {expect} from "vitest";

describe('Spec EmailLink', function () {
  it('it exists', () => {
    expect(EmailLink).to.be.ok;
  });
})