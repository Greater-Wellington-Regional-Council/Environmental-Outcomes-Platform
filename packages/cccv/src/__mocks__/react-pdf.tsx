import React from 'react'

export const PDFDownloadLink = ({ children }: { children: React.ReactNode }) => <div>{children}</div>

// eslint-disable-next-line
export const StyleSheet = {
  create: (styles: never) => styles,
}
